# Demo mode: the app runs entirely in the browser on seeded data.
# Leave this alone until the backend exists.
VITE_USE_MOCK=true

# When the backend is ready, set VITE_USE_MOCK=false and point this at it.
VITE_API_URL=http://localhost:8000
